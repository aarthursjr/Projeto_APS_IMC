import { Colors } from "../../constants/theme"

export const styles = {
    buttonWrapper: {
        justifyContent: "center",
        alignItems: "center",
        width: "100%",
        height: 64,
        borderRadius: 8
    },
    buttonText: {
        fontWeight: "bold",
        color: Colors.surface
    }
}