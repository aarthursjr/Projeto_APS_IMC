import { Colors } from "../../constants/theme";

export const styles = {
  navItem: {
    flexDirection: "column",
    alignItems: "center",
    padding: 10,
  },
  navItemLabel: {
    fontSize: 12,
  },
  navItemActiveLabel: {
    fontWeight: "bold",
    color: Colors.primary,
  },
};
