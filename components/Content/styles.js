import { colors } from "../../constants/theme";

export const styles = {
  container: {
    flex: 1,
    flexDirection: "column",
    padding: 20,
    paddingTop: 40,
    backgroundColor: colors.background,
  },
};
