import { colors } from "../../constants/theme";

export const styles = {
  container: {
    flexGrow: 1, 
    padding: 20,
    paddingTop: 40,
    paddingBottom: 40, 
    backgroundColor: colors.background,
  },
};