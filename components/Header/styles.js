import { Colors } from "../../constants/theme";

export const styles = {
  header: {
    alignItems: "center",
    padding: 20,
  },
  image: {
    width: 64,
    height: 64,
  },
  title: {
    fontSize: 24,
    fontWeight: "bold",
    color: Colors.textPrimary,
  },
  subtitle: {
    fontSize: 16,
    color: Colors.textSecondary,
  },
};
